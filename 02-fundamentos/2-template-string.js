const nombre = 'Deadpool';
const real = 'Wade Winston';

//las comillas esas se llaman -> backticks 
//${...} -> puedo ejecutar una funcion js
const template_string = `${nombre} ${real}`;

console.log(template_string);  //esto me muestra -> Deadpool Wade Winston
       